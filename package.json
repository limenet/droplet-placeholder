{
  "name": "droplet-placeholder",
  "dependencies": {
    "html-minifier-terser": "^7.2.0",
    "mustache": "^4.2.0",
    "water.css": "^2.1.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.5.0",
    "globals": "^17.7.0"
  },
  "scripts": {
    "build": "node src/build.js",
    "test": "node --test",
    "coverage": "node --test --experimental-test-coverage --test-reporter=spec --test-reporter-destination=stdout --test-reporter=lcov --test-reporter-destination=lcov.info",
    "watch": "node src/watch.js",
    "lint": "eslint src/ test"
  }
}
